# Server Configuration
server.port=8080

# Default profile is 'dev' which uses H2 in-memory database
spring.profiles.active=dev

# Logging
logging.level.org.springframework.web=INFO
logging.level.com.poker.agent=DEBUG
